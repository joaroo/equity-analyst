name: earnings-preview
description: Pre-earnings analysis pipeline for a single ticker.
version: "1.0"
trigger: "command:/earnings-preview"

pipeline:
  phase_1:
    steps:
      - subagent: earnings-preview-analyst
        inputs:
          ticker: "from:user-input"

error_handling:
  on_failure: "return error message to user"
