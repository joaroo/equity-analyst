{
  "connectors": {
    "market-data": {
      "primary": null,
      "status": "unbound — awaiting broker MCP connector",
      "purpose": "All real-time market data: prices, analyst ratings, earnings dates, macro indicators, sector performance"
    },
    "notifications": {
      "primary": "${NOTIFICATION_MCP_TOOL}",
      "purpose": "Progress updates and final report delivery. Set NOTIFICATION_MCP_TOOL to your provider's tool name (e.g. mcp__slack__slack_post_message).",
      "docs": "connectors/slack/CONNECTOR.md",
      "example_providers": {
        "slack": "mcp__slack__slack_post_message",
        "telegram": "mcp__telegram__send_message",
        "gmail": "mcp__gmail__send_email"
      }
    }
  },
  "example_config": ".mcp.json.example",
  "anti_patterns": [
    "Do not use WebSearch or WebFetch for market data — route through market-data connector",
    "Do not hardcode model names in skills or commands — use connector aliases only",
    "Do not call the notifications connector from read-only subagents — only portfolio-manager may write"
  ]
}
